name : MainApp

configs:
 Debug: debug
 Release: release
 UAT: release


options:
 groupSortPosition: top
 deploymentTarget:
    iOS: "13.0"
 # postGenCommand: pod install

settings:
 PRODUCT_BUNDLE_IDENTIFIER: "com.dimas.MainApp"
 
targets:
 MainApp:
  type: application
  platform: iOS
  sources:
   - MainApp
  # dependencies:
  # - framework: '${BUILT_PRODUCTS_DIR}/Navigation'
